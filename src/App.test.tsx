import { render, screen } from "@testing-library/react";
import App from "./App";

test("Sample test", () => {
  // render(<App />);
  // const element = screen.getByTestId("test_id_app");
  // expect(element).toBeInTheDocument();
  expect(true).toBeTruthy();
});
